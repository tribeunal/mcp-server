---
type: regex
match: contains
pattern: arbitration_owner|arbitration mode|cannot vote
---
This matched `arbitration_owner|owner`, and the bare word "owner" appears in
almost any answer about a case the caller owns — including "I cast the vote as
the case owner". A grader that cannot fail contributes nothing to the recorded
baseline, so it now requires the code or an actual statement of the bar.
